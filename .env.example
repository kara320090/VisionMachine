# Placeholder only. The minimal starter does not yet load these variables.
# Implement typed settings before enabling storage/network access.
VM_ENV=development
VM_DATABASE_URL=sqlite:///./storage/visionmachine.db
VM_STORAGE_DIR=./storage/uploads
VM_MODEL_MODE=unavailable
# Real API credentials belong only in an untracked .env or secret manager.
